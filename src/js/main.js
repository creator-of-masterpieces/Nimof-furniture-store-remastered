// Общий JS для сайта

// Инициализировал svg иконки
document.addEventListener('DOMContentLoaded', () => {
    lucide.replace({
        stroke: 'currentColor',
        fill: 'currentColor',
        width: 24,
        height: 24
    });
});